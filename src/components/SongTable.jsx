import React from 'react';

function SongTable(props) {
console.log(props)
  const filtered = filteredSongs.map(person =>  <div></div>); 
  return (
    <div>
      {filtered}
    </div>
  );
}

export default SongTable;